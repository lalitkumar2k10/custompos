<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="lks.list">
      <field name="name">lks list</field>
      <field name="model">lks.lks</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="lks.action_window">
      <field name="name">lks window</field>
      <field name="res_model">lks.lks</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="lks.action_server">
      <field name="name">lks server</field>
      <field name="model_id" ref="model_lks_lks"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="lks" id="lks.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="lks.menu_1" parent="lks.menu_root"/>
    <menuitem name="Menu 2" id="lks.menu_2" parent="lks.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="lks.menu_1_list" parent="lks.menu_1"
              action="lks.action_window"/>
    <menuitem name="Server to list" id="lks" parent="lks.menu_2"
              action="lks.action_server"/>
    -->
  </data>
</odoo>