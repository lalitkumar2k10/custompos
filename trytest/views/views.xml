<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="trytest.list">
      <field name="name">trytest list</field>
      <field name="model">trytest.trytest</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="trytest.action_window">
      <field name="name">trytest window</field>
      <field name="res_model">trytest.trytest</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="trytest.action_server">
      <field name="name">trytest server</field>
      <field name="model_id" ref="model_trytest_trytest"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="trytest" id="trytest.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="trytest.menu_1" parent="trytest.menu_root"/>
    <menuitem name="Menu 2" id="trytest.menu_2" parent="trytest.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="trytest.menu_1_list" parent="trytest.menu_1"
              action="trytest.action_window"/>
    <menuitem name="Server to list" id="trytest" parent="trytest.menu_2"
              action="trytest.action_server"/>
    -->
  </data>
</odoo>